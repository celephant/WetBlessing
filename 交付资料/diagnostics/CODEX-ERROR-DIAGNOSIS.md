# WetBlessing Codex 报错诊断与接续记录

本记录现已归入项目交付资料，作为早期故障诊断的历史记录保留。下文“项目当前状态”描述的是当时的状态，其中重复对白、生成器等问题后来已有修订，不能当作现状；最新进展见 ../README.md 与 ../checks/EXPERIENCE-REVIEW.md。

诊断基于本地日志的只读检查；未重放失败请求，未重载图片，未修改原件、Codex 配置或旧会话。文中的临时路径与会话 ID 仅保留作历史定位，不是当前交付文件的使用依赖。

## 已确认的事实

- 两次失败会话的客户端版本均为 `codex-cli 0.154.0`，来源为 `codex-tui`，provider 为 `custom`。
- 请求发送至 `https://api.openmodel.ai/v1/responses`，返回 HTTP 400，错误内容为 `invalid_request_error: invalid JSON body`。
- 第一段会话：`01a0b1da-5be0-7f12-9bf6-7dadb826238c`。最后一次成功请求在日志时间 2026-09-18 02:55:19 UTC；加入三张核验拼图后的请求于约 02:55:47 UTC 失败。会话历史的图片 data URL 字符数累计为 15,800,530，包含更早加载的图片。相关图片输出在会话 JSONL 第 765 行。
- 第二段会话：`01a0b27f-26be-76a0-957e-7fd7243b1b6c`。前三张拼图的图片 data URL 共 6,904,550 个字符（JSONL 第 87 行），之后请求成功；加入另外三张后累计为 14,800,096 个字符（第 97 行），约 03:17:32 UTC 请求失败。
- 上述字符数是本地会话中的图片编码数据统计，不是实际网络请求大小，也不证明接口具体限制为多少 MB。
- 两个 JSONL 文件均可逐行解析；其中 9 个和 6 个图片编码分别通过 Base64 检查，PNG 数据通过块 CRC 检查。
- 第二段会话在失败请求前的上下文日志记录 `total_usage_tokens=62305`、`auto_compact_scope_limit=244800`、`full_context_window_limit=258400`，并明确记录 `full_context_window_limit_reached=false`、`token_limit_reached=false`。两段会话未发现 compaction 记录。
- 在原会话发送“继续”后，接口仍返回同一 HTTP 400。
- 日志还出现客户端 0.154.0 与模型缓存 0.155.0 的版本不匹配警告，但没有证据证明这是本次错误的原因。

## 判断与未确认事项

优先怀疑图片历史累积后，大请求触发中转接口或上游的大小限制、截断或兼容问题。前三张图片在第二段会话中成功提交，削弱了“第一批图片本身坏了”的假设。客户端序列化或其他请求处理缺陷仍未完全排除。

现有日志没有失败请求的完整线上字节流，也没有中转服务端解析日志。因此不能认定具体请求大小上限，不能将本问题声称为已证实的 OpenAI 官方缺陷，也不能把规避方案说成已修复。

自动上下文压缩依赖的 token 门槛，与接口可能存在的请求字节门槛不同。这里 token 门槛未触发，不能指望它提前消除图片数据累积。

## 项目当前状态

- 项目根目录：`/Users/zezeng/Desktop/Saas/WetBlessing`。
- 当前工作目录和默认可写范围为 `public/assets`；修改根目录下的脚本、文档、清单需要遵守沙箱权限。
- 开工前阅读根目录 `AGENTS.md`、`docs/ENGINEERING-SPEC.md`、`docs/ASSET-RESET-PLAN.md`。
- 当前有大量既存工程清理改动，不能回滚或覆盖。
- `content/assets.manifest.json` 中 150 张图片和 1 个字体的 SHA-256 全部与文件相符。
- `public/assets/story.json` 有 83 个节点、188 条对白，但精确去重后仅 45 条；33 组对白重复。
- `scene.021`、`scene.026`、`scene.031`、`scene.081` 各有一条对白的 `speaker` 为 Kai、文本前缀为 Mia。
- 150 个图片记录与 64 个横竖配对候选均为 pending，没有已落盘的视觉核验结论。
- `scripts/generate-story.mjs` 仍按路径和索引推测人物并生成模板对白。只修改输出 JSON 后再次运行生成器，会覆盖人工修改；后续修正应处理这个来源问题。
- 本轮未执行 `npm run check` 或 `npm run build`；原件哈希校验通过不等于全部工程检查通过。

## 后续执行边界

1. 在明确恢复内容任务后，从纯文字交接开始，不恢复或分叉携带旧图片历史的失败会话。
2. 不再加载 `/tmp/wb-review-*.png` 大拼图。需要看图时仅在临时目录生成尺寸受控的 JPEG 预览副本；原图不得删改、重编码或改名。
3. 先记录预览实际字节数，再单批加载并落盘视觉观察。控制累计编码量，而不是仅控制一次调用的图片张数；具体预算是保守工作约束，不是已验证的接口限制。
4. 人物身份、画面细节和横竖配对不清楚时继续 pending；不能根据文件名或旧文档推定。低清预览看不清的细节不能标已核验。
5. 每批及时保存稳定资源 ID、观察、置信度及待核验项，以便下一段干净会话从文字结果继续。
6. 若要确认接口根因，应另做隔离、无业务图片的受控请求测试，或取得中转服务端日志；不要在业务会话中反复逼近失败门槛。
